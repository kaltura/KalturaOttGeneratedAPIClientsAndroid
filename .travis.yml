language: android
android:
  components:
    - tools
    - build-tools-28.0.3
    - android-28
script:
  - ./gradlew KalturaClient:build
after_success:
  - if [ -n "$TRAVIS_TAG" ]; then ./travis-push-to-bintray.sh; fi
notifications:
  email:
    recipients:
      - noam.tamim@kaltura.com
      - gilad.nadav@kaltura.com
    on_success: change
    on_failure: always
